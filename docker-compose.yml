services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@host.docker.internal:5432/multi_tenant_booking?schema=public"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
